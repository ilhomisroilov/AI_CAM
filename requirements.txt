# ============================================================
# AI_CAM - Industrial VIN detection & OCR system
# Target: Python 3.11 (Windows). Pinned to the versions verified on this machine.
#   py -3.11 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
# ============================================================

# --- Web backend ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
jinja2==3.1.4
python-multipart==0.0.9

# --- Deep learning ---
# Pinned to the build already installed & working here. ultralytics 8.2.58
# predates PyTorch's weights_only=True default (torch>=2.6), so backend/ai/detector.py
# applies a weights_only shim to load best.pt. (Alternatively: upgrade to
# `ultralytics>=8.3.0` for a native fix.)
torch==2.12.0
torchvision==0.27.0

# --- Computer Vision / AI ---
ultralytics==8.2.58          # YOLOv8 (best.pt) — loaded via weights_only shim
opencv-python==4.10.0.84     # rasmga ishlov berish, MJPEG kodlash
Pillow==10.4.0

# --- OCR: PaddleOCR (EasyOCR dan ko'chirildi) ---
# PaddleOCR engine + PaddlePaddle runtime. CPU build (default).
paddleocr==2.7.3
paddlepaddle==2.6.1          # CPU. GPU uchun pastdagi izohga qarang.

# numpy MUST stay on the 1.x line: opencv, torch, paddlepaddle are built against
# numpy 1.x — numpy 2.x causes ABI/runtime errors.
numpy==1.26.4

# --- Export ---
openpyxl==3.1.5              # Excel (.xlsx) eksport

# ------------------------------------------------------------
# GPU (Ubuntu + NVIDIA) — TAVSIYA ETILADI:
#   pip uninstall -y paddlepaddle
#   # CUDA 11.8 misol (o'z CUDA versiyangizga moslang):
#   pip install paddlepaddle-gpu==2.6.1 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# So'ng backend/config.py da OCR.use_gpu=True (default) — _resolve_gpu avtomatik
# CUDA ni tekshiradi. YOLO uchun DETECTION.device="auto" (cuda:0 ni tanlaydi).
#
# OFFLINE/SSL: PaddleOCR ham (EasyOCR kabi) ilk ishga tushganda modellarni
# yuklab oladi. Internet/SSL cheklangan zavodda modellarni oldindan yuklab,
# ~/.paddleocr/ ga joylashtiring (REPORT dagi izohga qarang).
# ------------------------------------------------------------
